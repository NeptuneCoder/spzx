<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.spzx.manager.mapper.BatchInfoMapper">

    <sql id="columns">
        id
        ,product_id,total_num,success_num,audit,status,start_time,end_time,remark,create_time,update_time,is_deleted
    </sql>
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into batch_info (id,
                                product_id,
                                total_num,
                                success_num,
                                audit,
                                status,
                                start_time,
                                end_time,
                                remark,
                                create_time,
                                update_time,
                                is_deleted)
        values (#{id},
                #{productId},
                #{totalNum},
                #{successNum},
                #{audit},
                #{status},
                #{startTime},
                #{endTime},
                #{remark},
                now(),
                now(),
                0)

    </insert>

    <resultMap id="batchMap" type="com.atguigu.spzx.model.entity.product.BatchItem"/>
    <select id="selectByProductId" resultMap="batchMap">
        select
        <include refid="columns"/>
        from batch_info
        where is_deleted = 0
        and product_id=#{id}
        order by id desc

    </select>

    <update id="updateById">
        update batch_info
        <set>
            <if test="totalNum!= null">total_num=#{totalNum},</if>
            <if test="successNum!= null">success_num=#{successNum},</if>
            <if test="audit!= null">audit=#{audit},</if>
            <if test="status!= null">status=#{status},</if>
            <if test="startTime!= null">start_time=#{startTime},</if>
            <if test="endTime!= null">end_time=#{endTime},</if>
            <if test="remark!= null">remark=#{remark},</if>
            update_time=now()
        </set>
        where id=#{id}
    </update>
</mapper>